import 'package:get/get.dart';

class InitialBinding extends Bindings {
  @override
  void dependencies() {
    // Services are already initialized in main(), so we don't need to do anything here.
    // This binding is kept for compatibility and future initialization needs.
  }
}
